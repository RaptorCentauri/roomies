const initialState = {
    matches: []
}

const matchesReducer = (state = initialState, action) => {
    switch (action.type) {
        case "":
        //SomeAction
    }
    return state
}

export default matchesReducer;